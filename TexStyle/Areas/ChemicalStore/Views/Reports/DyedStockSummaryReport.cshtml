@model IEnumerable<TexStyle.ViewModels.CS.Reports.DyeSummary_C1ViewModel>
@{
    ViewData["Title"] = "Dyed Summary Report";
    Layout = "_LayoutPrint";

}


    <div class="container">
        <div class="row">
            <div class="col-2 report-numberheading">
                C-1
            </div>


        </div>

        <div class="row">
            <div class="col-12 report-header1">
                @Model.FirstOrDefault().CompanyName
            </div>
        </div>
        <div class="row">
            <div class="col-12 report-header2">
                @ViewData["Title"]
            </div>
        </div>
        <div class="row">
            <div class="col-12 report-header3">
                DATE FROM: @Model.FirstOrDefault().DateFrom TO: @Model.FirstOrDefault().DateTo
            </div>
        </div>


    </div>
    <div class="table-responsive">
        <table class="table table-responsive">
            <thead>
                

  
                <tr class="report-detailheader sticky-top">
                    <th width="20%">Item</th>
                    <th width="20%"></th>
                    <th width="20%">Qty</th>
                    <th width="20%"></th>
                    <th width="20%"></th>


                </tr>
            </thead>
            <tbody>
                @* @if (Model != null)
                    {*@


                @if (Model.Count() > 0)
                {
                    @foreach (var detail in Model)
                    {
                        <tr>
                            @*<td colspan="2" class="report-detail-footer ">@detail.Name</td>
                            <td colspan="2" class="report-detail-footer ">@detail.Balance</td>
                            <td></td>*@





                        </tr>


                    }}


            </tbody>
        </table>
    </div>

