@model IEnumerable<TexStyle.ViewModels.PPC.Reports.P3DailyProductionReportViewModel>
@{
    ViewData["Title"] = "Dyed Summary Report";
    Layout = "_LayoutPrint";

}

<div class="table-responsive">
    <table class="table table-responsive">
        <thead>
            <tr>

                <th scope="col" width="20%" class="table-active report-numberheading">P-4</th>
                <th scope="col" width="20%"></th>
                <th scope="col" width="20%"></th>
                <th scope="col" width="20%"></th>
                <th scope="col" width="20%"></th>

            </tr>
        </thead>
        <tbody>
            @if (Model != null)
            {


                <tr>
                    <td class="report-header1" colspan="5">@Model.FirstOrDefault().CompanyName </td>
                </tr>

                <tr>
                    <td class="report-header2" colspan="5">@ViewData["Title"] </td>
                </tr>

                <tr>
                    <td class="report-header3" colspan="5">DATE FROM: @Model.FirstOrDefault().DateFrom TO: @Model.FirstOrDefault().DateTo</td>
                </tr>
                <tr>
                    <td colspan="5"> </td>
                </tr>
                <tr>
                    <td colspan="5"> </td>
                </tr>
                <tr class="report-detailheader">
                    <td>Date</td>
                    <td>Ecru(Kgs)</td>
                    <td>Ecru Cones</td>
                    <td>Dyed(Kgs)</td>
                    <td>Dyed(Bags)</td>


                </tr>

                @if (Model.Count() > 0)
                {
                    foreach (var grp in Model)
                    {
                        <tr>
                            <td colspan="5">

                                <strong>@grp.Date</strong>
                            </td>
                        </tr>
 

                        @*<tr>
                           
                            <td class="report-Subdetailheader">T. Ecru Kg </td>
                            <td class="report-Subdetailheader">T. Ecru Cones </td>
                            <td class="report-Subdetailheader">T. Dyed Kg </td>
                            <td class="report-Subdetailheader">T. Dyed Bags </td>
                            <td ></td>

                        </tr>*@

        <tr>
            <td></td>
            @*<td>@grp.EcruTotal</td>
            <td>@grp.ConesTotal</td>
            <td>@grp.DyedTotal</td>
            <td>@grp.BagsTotal</td>*@


        </tr>


                    }


                }
            }

        </tbody>
    </table>
</div>

